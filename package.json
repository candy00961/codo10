// package.json
{
  "name": "codo10",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "import": "node ./contentful/import.js" // Assuming you have this script for Contentful import
  },
  "dependencies": {
    // Moved from devDependencies - needed for runtime data fetching
    "contentful": "^10.15.1", // Kept specific version, use ^ if desired

    // Added - required by Netlify functions
    "node-fetch": "^2.7.0",  // Use v2 for CommonJS require() compatibility
    "pdf-lib": "^1.17.1",    // Added dependency for invoice generation

    // Existing runtime dependencies
    "markdown-to-jsx": "^7.4.7",
    "next": "15.1.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "node-cache": "^5.1.2" // Keep if cache-handler.js might be used
  },
  "devDependencies": {
    // Build/Analysis tools - moved from dependencies
    "@next/bundle-analyzer": "latest",
    "@netlify/plugin-lighthouse": "6.0.1", // Build plugin

    // Stackbit specific
    "@stackbit/cms-contentful": "^1.0.0", // Use specific version from lock file or latest compatible
    "@stackbit/types": "^2.0.0",         // Use specific version from lock file or latest compatible

    // Build tools
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.3",

    // Contentful import script dependency
    "contentful-import": "^9.4.39", // Use specific version from lock file or latest compatible

    // Local development utility
    "dotenv": "^16.4.5",

    // Linting
    "eslint": "^8.57.1", // Use specific version from lock file or latest compatible
    "eslint-config-next": "^15.0.0", // Match Next.js major version

    // Webpack related (often dev only)
    "source-map-loader": "^3.0.0" // Use specific version from lock file or latest compatible
  }
}
